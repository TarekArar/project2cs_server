{
  "routes": [
    {
      "method": "GET",
      "path": "/reserves",
      "handler": "reserve.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/reserves/count",
      "handler": "reserve.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/reserves/:id",
      "handler": "reserve.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/reserves",
      "handler": "reserve.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/reserves/:id",
      "handler": "reserve.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/reserves/:id",
      "handler": "reserve.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
